spring:
  profiles:
    active: dev           # 激活开发环境。部署到生产时，可以改为 prod，或者通过启动命令指定
  threads:
    virtual:
      enabled: true       # 开启 JDK 21 虚拟线程支持（公共配置）

server:
  port: 8080              # 统一的服务端口（公共配置）